name: test1
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter
  device_preview: ^1.2.7
  camera: ^0.11.3
  tflite_flutter: ^0.11.0
  flutter_tts: ^4.0.2
  image: ^4.1.7
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/yolov5n.tflite
    - assets/labelmap.txt